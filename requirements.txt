fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic>=2.0

# Face similarity — buffalo_sc MobileNet ~100 MB
insightface==0.7.3
onnxruntime==1.19.2
# onnx 1.16.x is the last version compatible with ml_dtypes < 0.4
onnx==1.16.2
ml_dtypes==0.3.2

# OCR — pure ONNX, no PyTorch/TF
rapidocr-onnxruntime==1.3.24

# Mood — DeepFace with opencv backend only (no dlib, no MTCNN, no moviepy)
deepface==0.0.93
tf-keras==2.16.0

opencv-python-headless==4.10.0.84
numpy==1.26.4
requests==2.32.3
